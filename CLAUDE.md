# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트

**quiz by quiz** — 브라우저에서 바로 도는 4지선다 상식 퀴즈 게임. TypeScript + 순수 HTML/CSS.
프레임워크도 번들러도 쓰지 않는다 — `tsc` 가 `.ts` 를 **ES 모듈 `.js` 로 옮길 뿐**이고 브라우저가 그것을 그대로 읽는다.

`docs/quiz-game-prd.md`가 **유일한 기준 문서**다. 기능은 FR 번호(FR-3.8 등)로 참조하며, 코드 주석도 그 번호를 단다.
PRD와 사용자 지시가 충돌하면 임의로 판단하지 말고 **먼저 물어본다.**

`docs/quiz-game-claude-code-prompts.md`가 3단계 개발 계획이다. **구현과 배포 모두 완료.**

| 단계 | 범위 | 상태 |
| --- | --- | --- |
| 1 | 기반 + 퀴즈 코어 (출제·타이머·즉시 피드백) | 완료 |
| 2 | 결과 화면 + 점수 + 저장소 어댑터 + 랭킹 + 전체 모드 | 완료 |
| 3-A | 문제 은행 40문제 (카테고리당 10) | 완료 |
| 3-B | 반응형·접근성 + 예외 처리 + 효과음 + 배포 준비 | 완료 |

서비스 이름은 **quiz by quiz**. `perust/quiz-by-quiz` 저장소의 `main`이 GitHub Pages로 나간다
(`.github/workflows/pages.yml`). 워크플로가 `npm ci` → `npm run build` 를 돌린 뒤
**올릴 것만 골라 담는다** (`index.html` · `css` · `data` · `js` · `src`).
**타입이 깨지면 거기서 멈춰 배포되지 않는다.**

한때 저장소 루트를 통째로 올렸고(`SOURCE_DIR: .`) 그래서 «루트에 둔 것이 전부 공개된다»는
규칙이 있었다. 지금은 **담은 것만 나가므로** `node_modules/` 도 `teacher/` 도 저절로 빠진다
(`.gitignore` 는 그대로 두었다 — 커밋 자체를 막는 것은 여전히 필요하다).
`src/` 를 함께 올리는 것은 소스맵이 그것을 가리키기 때문이다. 개발자 도구에서 `.ts` 원본이 보인다.
옛 React 구현은 `legacy/quiz-challenge/`에 아카이빙돼 있고 배포에는 쓰이지 않는다.

**이번 단계 범위 밖의 기능은 미리 만들지 않는다.** 2단계 것을 1단계에서 앞당겨 짓지 말 것.

## 실행과 확인

```bash
npm install                     # 처음 한 번 (typescript 하나뿐이다)
npm run build                   # src/*.ts → js/*.js
npm run watch                   # 고치는 동안 켜 둔다
python3 -m http.server 8765     # 프로젝트 루트에서. 포트는 아무거나
```

**`js/` 는 산출물이라 커밋하지 않는다** (`.gitignore`). 받아 온 직후에는 비어 있으므로
`npm run build` 를 먼저 돌려야 화면이 뜬다. 안 돌리면 `index.html` 의 부팅 감지가
3초 뒤 안내 화면을 띄운다 — 앱이 죽은 것이 아니라 아직 빌드하지 않은 것이다.

`file://`로 열면 CORS 때문에 ES 모듈과 `fetch`가 모두 막힌다. **파일을 직접 여는 것을 전제로 설계하지 말 것.**

린트·테스트 러너는 없다(의도적). **타입 검사가 그 자리를 대신한다** — `npm run check`.

화면 흐름은 `tools/check-screens.js` 를 **콘솔에 붙여넣어** 한 번에 훑는다. 화면을 옮긴 뒤
«아무것도 새지 않는가»를 56가지로 확인한다 — 퀴즈 세션이 살아남았는지(시간을 25초 앞당겨
깨워 본다), 손가락 조작부가 따라왔는지, 대화상자나 피드백 시트가 남았는지, 방향키의 임자가
맞는지. **여기 있는 검사는 모두 실제로 났던 버그에서 왔다.** 새 기능을 덮는 것이 목적이
아니라 같은 데를 두 번 물리지 않는 것이 목적이니, 물릴 때마다 한 줄씩 더한다.
고친 자리를 일부러 되돌려 **검사가 실제로 잡는지 확인하고 넣었다.**

그 밖의 검증은 브라우저에서 직접 하고, 문제 데이터는 이 정도로 훑는다:

```bash
for f in data/*.json; do
  python3 -c "import json;d=json.load(open('$f'));print('$f',len(d),'OK' if all(0<=q['answerIndex']<len(q['choices'])==4 for q in d) else 'FAIL')"
done
```

## 기술 제약 (사용자가 정한 것, 바꾸지 말 것)

- **번들러 없음. 프레임워크·런타임 라이브러리 없음.** npm 의존성은 개발용 `typescript` 하나뿐이고 배포물에 들어가지 않는다
- ES 모듈(`<script type="module">`)과 `fetch` 사용. **`import` 에는 확장자 `.js` 를 붙인다** — 번들러가 없어 브라우저가 그 경로를 그대로 가져간다 (TS 가 `.js` 를 `.ts` 로 이어 준다)
- `localStorage` 외의 브라우저 저장소 금지
- UI 문구와 주석은 한국어
- **`strict: true` 다.** `any` 로 도망가지 않는다 — 저장소나 네트워크에서 읽은 것처럼 정말 모르는 값은 `unknown` 으로 받아 검증하며 좁힌다. 불가피하게 단언할 때는 왜 안전한지 그 자리에 적는다

## 구조

```
index.html          화면마다 <section data-screen="...">. 해시 라우팅 없음 → 새로고침하면 항상 홈
src/types.ts        층을 넘어 다니는 값의 모양. 한 파일 안에서만 쓰는 것은 그 파일에 둔다
src/dom.ts          요소를 집는 자리. need()는 없으면 던지고 maybe()는 없어도 된다
src/constants.ts    제한 시간·출제 수·배점·카테고리 정의. 상수는 여기 한 곳에만 둔다
src/core/           게임 로직. DOM을 절대 건드리지 않고 입력을 받아 값을 반환한다
src/ui/             DOM 렌더링. 게임 규칙을 갖지 않는다
src/storage/        랭킹·설정 저장소. adapter.ts가 구현체를 고르고, safe-storage.ts만 localStorage를 만진다
src/online/         온라인 방 저장소. 같은 방식이고, 저장은 storage/safe-storage.ts를 거친다
src/data/           JSON 로드 + 형식 검증
src/app.ts          위를 잇는 진입점. 화면 흐름만 담당
data/<카테고리>.json  문제 은행. 파일명 = 카테고리 코드
js/                 tsc 산출물. **고치지 말 것** — 다음 빌드에 덮인다
```

**이 경계가 이 프로젝트의 핵심 설계다** (PRD 9: v2에서 프레임워크로 옮길 때 로직을 재사용하기 위함).
`core/`에 `document`나 `window`가 등장하면 잘못 짠 것이다.

## 건드리기 전에 알아야 할 것

**타이머 (`core/timer.ts`)** — `setInterval` 카운트다운이 아니다. 시작 시각을 찍어두고 물어볼 때마다 `now - startedAt`을 다시 계산한다. `stop()`은 무언가를 멈추는 게 아니라 멈춘 시각을 기록하는 것이다. 그래서 해설 중 시간이 안 흐르고, 탭이 백그라운드에 있어도 어긋나지 않는다 (FR-3.11).
숨은 탭에서는 rAF가 멈춰 **표시만** 정지하고, 복귀 시 실제 경과로 즉시 보정된다 — 이건 버그가 아니라 설계다. 카운트다운 방식으로 "고치지" 말 것.

**보기 순서** — `sampler.shuffleChoices()`가 보기를 섞으면서 `answerIndex`를 새 위치로 옮긴다. 화면에 뜬 문제의 `answerIndex`는 JSON의 값과 다르다. 채점은 반드시 섞인 뒤의 객체로 한다.

**출제 수** — `QUESTIONS_PER_ROUND`(10)는 은행 크기와 무관한 상수다 (FR-1.2). 은행이 더 적으면 있는 만큼만 낸다. 3-A에서 카테고리당 10문제를 채워 지금은 코드 수정 없이 `1/10`으로 돈다. 은행을 더 키워도 출제 수는 10으로 유지되고, 그때부터 판마다 문제 세트가 달라진다 (PRD FR-1 각주의 "고정 세트 챌린지" 검토 시점).

**중복 응답** — `session.submit()`은 같은 문항에 두 번째로 들어온 호출을 무시하고 첫 기록을 돌려준다 (FR-3.3). 클릭과 시간 초과가 겹쳐도 안전하다.

**모달 금지** — `window.confirm` / `alert` / `prompt`를 쓰지 않는다. 나가기 확인, 랭킹 초기화 확인, 게임 모드 조작법은 `index.html`의 인페이지 다이얼로그이며, Tab이 밖으로 새지 않게 가두고 닫을 때 원래 자리로 포커스를 되돌린다. 브라우저 모달은 자동화 검증을 블로킹하고 접근성에도 걸린다.

다이얼로그가 열려 있으면 **시간 초과가 포커스를 가져가지 않는다** (`showFeedback`의 마지막 줄). 시간은 다이얼로그 뒤에서도 계속 흐르는데(FR-3.12), 그때 «다음 문제»로 포커스를 옮기면 갇혀 있어야 할 포커스가 밖으로 새고 Tab이 다이얼로그를 벗어난다.

**부팅 감지** — `index.html`의 인라인 스크립트가 3초 타이머를 걸고, `app.ts`가 뜨면 `markBooted()`가 끈다. 타이머가 살아 있으면 스크립트 자체가 막힌 것이라(`file://`로 열었을 때가 대표적) 안내 화면이 뜬다. 모듈이 로드되지 않는 상황은 JS로 손쓸 수 없어 HTML에 정적으로 심어 둔 것이다. `.app`은 `is-booted` 클래스가 붙어야 보인다.

**걷는 캐릭터 (`ui/walker.ts`)** — 퀴즈 무대·홈·내 캐릭터·로비·대기실·결과·랭킹이 **모두 같은 장치**를 쓴다.

**워커는 한 가지 방식뿐이다.** 캐릭터는 `position: fixed`로 화면에 붙어 **페이지 전체를 걸어 다니고**, Space/Enter를 누르면 발밑을 **진짜로 누른다**(`element.click()`).

한때 «무대 방식»(`stage` 안에서만 움직이고 `getZones()`가 준 칸 번호를 넘기는 것)이 함께 있었으나, 퀴즈 무대까지 옮긴 뒤로 쓰는 곳이 없어져 걷어냈다. **되살리지 말 것** — 발밑을 그때그때 보는 방식이 칸 목록을 유지하는 것보다 단순하고, 화면 쪽에 «무엇을 골랐는지»를 잇는 코드도 필요 없다.

| | 값 |
| --- | --- |
| 발밑 | `document.elementFromPoint`로 **그때그때** 본다 |
| «밟을 수 있는 것» | `pickable` 선택자. 기본은 버튼·링크·입력칸 |
| 밟은 표시 | `is-standing` 클래스 (`standClass`로 바꿀 수 있다) |
| 고르기 | 발밑 요소의 `click()`. 글자를 적는 칸만 `focus()` |
| 스크롤 | 가장자리에 닿은 채 더 밀면 페이지가 움직인다 |

**칸 목록을 만들지 않는다.** 카드를 더하거나 빼도 손댈 곳이 없고, 고르는 것이 진짜 `click()`이라 **버튼에 달린 리스너가 마우스로 눌렀을 때와 똑같이 움직인다.** 그래서 «걸어가서 Enter»와 «손가락으로 누르기»가 한 길로 모인다 — 화면 쪽에 선택 처리를 두 벌 두지 않는다.

**`.walker`의 `pointer-events: none`을 떼지 말 것.** 떼는 순간 `elementFromPoint`가 캐릭터 자신을 집어 발밑이 늘 «없음»이 된다. 화면에 띄운 조작부(스틱·확정 버튼)도 발밑에서 걸러낸다 — 늘 같은 자리에 떠 있어서 그 위에 섰다고 «고를 수 있다»고 보면 안 된다.

**두 경우에는 워커가 키를 받지 않는다** (`isBlocked`). ① 글자를 입력하는 중 — 결과 화면 닉네임 칸에서 이름을 적는 동안 캐릭터가 같이 걸어가면 안 된다. ② 다이얼로그가 열려 있을 때 — 그 안이 전부여야 하는데 뒤에서 캐릭터가 걸어 다니면 가려진 자리를 밟게 되고 갇혀 있어야 할 조작이 밖으로 샌다.

**글자를 적는 칸도 밟아서 들어간다.** 버튼만 밟을 수 있으면 걸어 다니는 사람에게는 로비의 방 코드도 대기실의 채팅칸도 갈 수 없는 곳이 된다. 다만 거기서는 «누르기»가 아니라 커서를 넣는다(`focus()`) — `click()`은 커서를 넣어 준다는 보장이 없고, 무엇보다 **누르는 것과 들어가는 것은 뜻이 다르다.** 체크박스·라디오처럼 커서가 머물지 않는 `input`은 그대로 누른다 (`TAP_INPUTS`).

**«제 키를 스스로 쓰는 것»에 포커스가 있으면 워커는 비켜난다.** 버튼만 비켜 주면 모자란다 — 체크박스는 Space가 곧 조작인데, 워커가 가로채면 `preventDefault`가 그 기본 동작까지 죽인다. 실제로 그래서 **키보드만 쓰는 사람은 「비공개로 만들기」를 켤 수 없었고**(Enter는 폼 제출이라 우회로도 없다), 그 Space가 대신 발밑의 「만들기」를 눌렀다. 무엇이 그런 것인지는 `pickable`이 이미 알고 있으므로 그것으로 판정한다.

**들어갔으면 나올 길이 있어야 한다.** 커서가 들어간 순간 방향키는 그쪽 것이 되어 캐릭터가 멈춘다. `Esc`로 나오고, 손가락에는 Esc가 없으므로 **확정 버튼이 그동안 「나가기」가 된다.**

이 `Esc`는 `walker.ts`가 **캡처 단계에서 받아 화면 쪽으로 넘기지 않는다.** 화면들은 `Esc`를 «뒤로 가기»로 쓰는데(로비 → 홈), 방 코드를 적다 누른 사람이 홈으로 튕겨 나가면 적던 것이 통째로 날아간다. **다이얼로그 안은 예외다** — 거기서는 `Esc`가 «취소»이고 갇힌 상태에서 나가는 유일한 길이라 다이얼로그에 양보한다. 화면마다 따로 두지 말 것: 한때 대기실만 제 `Esc`를 갖고 있었고, 그래서 대기실만 규칙이 다른 화면이었다.

**위쪽 한계는 풀어 둔다.** 머리가 솟지 않도록 캐릭터 높이만큼 띄우면 **화면 맨 위의 버튼에 발이 닿지 않는다** — 앱 바가 딱 그 높이에 있다. 대신 맨 위에서는 머리가 화면 밖으로 조금 잘린다.

**밟은 표시가 없는 버튼에는 아웃라인을 준다** (`.button.is-standing` 등). 카드류는 제 색이 있지만 일반 버튼은 아무 표시가 없어, 걸어 올라서도 «누를 수 있다»는 것이 보이지 않는다. 포커스 테두리와 같은 모양을 쓴다 — 뜻이 같기 때문이다.

**결과·랭킹처럼 «걸어가서 버튼을 누르는 것»이 전부인 화면은 `ui/screen-walker.ts`를 쓴다.** 홈과 내 캐릭터는 저마다의 사정(처음 설 자리, 밟으면 미리보기)이 있어 직접 워커를 만든다.

**한 번에 하나만 켠다.** 키와 스틱은 모듈 하나에 모아두고 지금 켜진 워커에게만 전달한다. 화면을 떠날 때 `setEnabled(false)`를 부르지 않으면 캐릭터가 안 보이는 화면에서 계속 뛴다.

**그래서 워커를 쓰기 직전에는 반드시 `setEnabled(true)`를 부른다.** 다른 화면의 워커가 켜지는 순간 이쪽은 조용히 꺼져 있다. 한 번 켜두고 「초기화」만 하면 아무 일도 일어나지 않는다 — 실제로 홈 워커를 넣으면서 무대가 그렇게 죽었다(캐릭터가 배치되지 않고, 스틱이 안 뜨고, 바닥을 눌러도 답이 안 나갔다). `setEnabled(true)`는 이미 켜져 있어도 안전하고 자리·입력을 처음 상태로 되돌리므로, 초기화가 필요한 자리에서도 이걸 부른다. 별도의 `reset`을 두지 않는 이유다.

**닉네임은 홈 맨 위 카드에서 바꾼다.** 랭킹과 온라인 방이 같은 이름을 쓰므로 한 곳에서 정한다.
기본값은 `DEFAULT_NICKNAME`(퀴즈왕) — 비워 두면 결과 화면에서 매번 새로 지어야 하고 온라인 방에서는
이름 없는 사람이 된다. 바꾸는 자리는 인페이지 다이얼로그다(`window.prompt` 금지). 열려 있는 동안에는
워커가 키를 받지 않으므로(`isBlocked`) 캐릭터가 뒤에서 걸어 다니지 않는다.

**방 안에서 이름이 겹치면 뒤에 1부터 숫자를 붙인다** (`rules.ts`의 `uniqueNickname`).
같은 이름이 둘이면 누가 누구인지, 말풍선이 누구 것인지 알 수 없다. **거절하지 않고 조용히 비켜 준다** —
방에 들어가려는 사람에게 이름을 고쳐 오라고 돌려보낼 일이 아니다. 빈 번호가 있으면 그것을 채운다.

**홈에서는 게임 모드와 무관하게 늘 걸어 다닌다.** 버튼은 그대로 눌러도 되므로 걷기는 «또 하나의 길»이고, 게임 모드 조작을 미리 익히는 자리다. 캐릭터는 「내 캐릭터」 칸 위에서 시작하고, 서 있을 때도 제자리에서 뛴다 — 움직일 수 있다는 것을 알리는 장치라 없애지 말 것.

**캐릭터 (`characters.ts`, `ui/sprite.ts`)** — **`name`을 화면에 그리지 않는다.** 무엇으로 보이는지는 보는 사람이 정한다. 다만 스크린리더에는 `aria-label`로 이 값을 알려준다 — 그림만 있는 버튼은 눈으로 볼 수 없는 사람에게 아무것도 아니다. **눈으로 보는 사람에게만 감추는 것이 목적이지, 이름을 없애는 것이 아니다.** 슬라임은 CSS 그라디언트, 도트 몬스터는 `grid` 문자열을 `box-shadow`로 펼쳐 그린다. **이미지 파일을 쓰지 않는다.** 점 하나에 여러 그림자를 얹는 방식이라 스프라이트 전체가 요소 하나이고, 그래서 뛰고 눌리는 동작이 도트에도 그대로 먹는다. `PIXEL_UNIT`은 점의 크기이자 간격이다 — `.walker__dot`의 `width`와 어긋나면 픽셀 사이에 틈이 생긴다.

캐릭터를 더하려면 `CHARACTERS` 배열에 넣기만 하면 된다. 화면·저장·무대가 모두 따라온다. **`id`를 바꾸면 그 캐릭터를 쓰던 사람의 선택이 풀린다** — `findCharacter`가 못 찾으면 기본값으로 되돌리므로 앱이 죽지는 않는다.

**게임 모드 (`ui/arena.ts`)** — 앱 바의 🕹️ 토글로 켜는 **화면 옵션이다.** 퀴즈퀴즈(큐플레이) 방식으로, 십자로 나눈 2×2 바닥 위를 캐릭터가 **자유롭게 돌아다니고 시간이 다 됐을 때 밟고 있는 칸이 답**이 된다. 밟은 칸에는 불이 들어온다. 판 도중에 켜고 꺼도 세션이 유지된다.

**방 설정으로 켠 게임 모드는 판이 끝나면 되돌린다** (`app.ts`의 `restoreMyGameMode`). 방 설정은 **그 판의 것이지 내 설정이 아니다.** `createToggle`의 `set`은 `onChange`를 부르지 않아 저장되지 않으므로, 되돌리지 않으면 앱 바만 켜진 채 남고 **저장된 값과 어긋나 새로고침 한 번에 화면이 뒤집힌다.** 홈에서 카테고리를 고르면 켠 적 없는 게임 모드로 판이 열리기도 했다. 되돌리는 자리는 `goHome`과 `openWaitingRoom` 두 곳이다 — 판이 끝나는 모든 길이 둘 중 하나를 지난다. 대기실의 「모드」 버튼은 방 설정을 그대로 보여주므로 앱 바까지 그 값을 들 이유가 없다.

**판 도중에 켤 때, 이미 답을 낸 문항이면 다시 잠근다** (`quiz.ts`의 `setGameMode` → `arena.lock()`). `setEnabled(true)`는 잠금을 푸는 것이 일이라, 그냥 켜면 채점이 끝난 바닥 위를 캐릭터가 다시 걸어 다니고 손가락 조작부가 «다음 문제» 버튼을 가린다. 칸에 칠한 정답·오답은 그대로 두므로 `reset`은 부르지 않는다.

**이 모드만 시간 초과의 뜻이 다르다.** 보통 모드는 시간이 끝나면 무조건 오답이지만(FR-3.10), 게임 모드는 `quiz.ts`의 `handleTimeout`이 `arena.standingIndex()`를 `session.submit`에 넘긴다. `core/session.ts`는 그대로다 — `choiceIndex`가 `null`이면 시간 초과라는 계약을 이미 갖고 있어서, 밟은 칸이 없으면 지금까지와 똑같이 동작한다.

**무대 밖으로도 걸어 나가 화면의 아무 버튼이나 밟고 누를 수 있다.** 나가기·`?`·앱 바 토글까지 전부 밟힌다.

**그래도 규칙은 그대로다. «밟을 수 있는 것»과 «답으로 세는 것»이 다르기 때문이다.** `pickable`은 넓게 열어 두고, 답으로 세는 것은 `indexOfNode`가 바닥 칸과 위 보기 버튼만 번호로 돌려준다. 그래서

- 바닥 칸 위에서 시간이 끝나면 그 칸이 답
- 위 보기 버튼 위에서 끝나면 같은 번호로 본다 — 바닥으로 내려가지 않고 답해도 결과가 같다
- 나가기나 여백 위에서 끝나면 아무 칸도 밟지 않은 것이 되어 **시간 초과 오답**

십자 한가운데에서 시작하는 공정성 장치도 그대로다 — 가만히 있으면 여전히 아무 칸도 밟지 않는다.

**캐릭터는 문항마다 십자 한가운데(중립)에서 시작한다. 이 자리를 바꾸지 말 것.** 가만히 있으면 아무 칸도 밟지 않아 시간 초과 오답이 되고, 그래서 보통 모드와 난이도가 같아진다. 칸 위에서 시작하면 아무것도 하지 않은 사람이 25%를 거저 얻고, 두 모드가 **같은 랭킹 키에 쌓이므로** 기록이 오염된다.

**«다음 문제» 버튼의 `Enter ↵` 표시** — 게임 모드에서는 손이 방향키에 있으므로 마우스로 옮겨 가지 않아도 된다는 것을 버튼에서 알린다. 키보드가 없는 기기에는 쓸모가 없어 `@media (pointer: fine)`에서만 그린다.

이 표시 때문에 두 가지가 딸려 있다. **버튼 글자는 `#next-label` span에만 쓴다** — 버튼째 `textContent`로 갈아치우면 키 표시가 지워진다. 그리고 `quiz.ts`가 **Enter를 직접 처리한다**: 원래는 버튼에 포커스가 있을 때만 브라우저가 눌러주는데, 사용자가 여백을 클릭해 포커스가 빠지면 버튼에 적힌 말이 거짓이 된다. 어떤 버튼에든 포커스가 있으면 건드리지 않고 브라우저에 맡긴다.

**손가락 입력에는 이동 스틱을 그린다** (`.walk-stick`, `walker.ts`가 관리한다). `@media (pointer: coarse)`에서만 나타나고 `position: fixed`라 세로 자리를 차지하지 않는다. 민 거리가 그대로 속도가 되므로(방향키는 켜짐/꺼짐뿐이다) 살살 밀면 천천히 움직인다. 손잡이가 밀려나는 최대 거리는 값을 박아두지 않고 `(스틱 폭 − 손잡이 폭) / 2`로 뽑는다 — 짧은 화면에서 스틱이 작아지기 때문이다.

**반대편에는 확정 버튼(`.walk-confirm`)을 둔다.** 손가락에는 Enter가 없다. 이게 없으면 스틱으로 걸어가 놓고도 결국 그 칸을 직접 눌러야 해서 스틱이 헛돈다. 왼쪽 스틱·오른쪽 확정으로 두 엄지가 나뉜다. 하는 일은 Enter와 똑같이 `pick()`이다.

**스틱에 `z-index`를 주지 말 것.** 이 프로젝트에는 `z-index`가 하나도 없어서 다이얼로그(`<main>` 뒤에 온다)가 DOM 순서로 위에 온다. 스틱에 `z-index`를 주면 그 순서가 깨져 다이얼로그 위로 올라온다.

**조작부를 CSS 선택자로 숨기지 말 것.** 스틱과 확정 버튼은 `position: fixed`라 **어느 화면의 자손도 아니다.** 한때 `.arena--locked .walk-stick`으로 치웠는데 스틱을 `.arena` 밖으로 옮긴 뒤로 매칭되지 않는 죽은 규칙이 됐고, 모바일에서 답을 낸 뒤 스틱이 «다음 문제»를 가렸다. 지금은 잠금 상태를 아는 `walker.setLocked`가 `--on` 클래스를 떼어 치운다.

이동은 `requestAnimationFrame` 루프에서 초당 `SPEED`px씩 적분한다. 밟을 칸 목록을 들고 있지 않고 **발밑에 실제로 무엇이 있는지**를 `elementFromPoint`로 그때그때 본다. 그래서 바닥을 다른 모양으로 깔아도, 화면에 무엇을 새로 놓아도 이동 코드는 그대로 맞는다. 대각선은 두 방향키를 함께 누른 것이고 길이를 1로 맞춰 더 빠르지 않게 한다. 눌린 키는 `held`에 모으며, 창을 벗어나면 `keyup`을 놓칠 수 있어 `blur`에서 비운다.

**화면이 캐릭터를 따라온다** (`pushScroll`). 가장자리에 **닿았을 때가 아니라 가까워지면** 민다 — `CAMERA_MARGIN`(120px, 짧은 화면에서는 화면 높이의 ¼) 안으로 들어오는 순간부터다. 닿아야 밀리면 **벽을 미는 느낌**이고 가는 쪽이 보이지 않는다.

**민 만큼 캐릭터를 되돌린다.** 이게 이 방식의 전부다. 되돌리면 캐릭터가 화면 그 자리에 머물고 배경만 흐르므로 **목표가 캐릭터 쪽으로 다가온다.** 되돌리지 않으면 캐릭터와 화면이 **같은 방향으로 함께** 움직여, 가장자리 가까이 있는 것이 다가가는 만큼 멀어져 영영 잡히지 않는다 — 한때 그래서 «여백에서 미는 것» 자체를 금지했었다. 문제는 여백이 아니라 되돌리지 않은 것이었다.

스크롤이 끝에 닿으면 실제로 밀린 양이 0이라 되돌릴 것도 없다. 그때는 캐릭터가 그대로 나아가 화면 가장자리까지 간다 — **페이지 맨 아래의 버튼도 밟힌다.**

`place()`에서는 부르지 않는다. 화면에 들어올 때마다 캐릭터 쪽으로 스크롤이 끌려가면 위에서부터 읽어야 할 글이 밀려난다.

사용자가 스스로 스크롤해도 캐릭터는 그대로이고 발밑만 바뀐다. 루프는 멈춰 있을 수 있으므로 `scroll`에서 `refresh()`로 표시를 맞춘다.

`pos`는 화면 기준이고 `elementFromPoint`가 받는 것도 화면 기준이라, **스크롤이 움직여도 발밑 판정은 어긋나지 않는다.** 실제로 스크롤이 밀린 상태에서 발밑의 카드와 «서 있다»고 표시된 것이 같은지 확인했다.

**`placed` 플래그** — 화면이 아직 보이지 않을 때 무대를 켜면 크기가 0으로 잡혀 중앙에 세울 수 없다. 그러면 `placed`가 `false`로 남아 루프와 `resize` 어느 쪽으로 들어오든 다시 세운다. 이게 없으면 클램프만 걸려 캐릭터가 1번 칸에 서 버리고, **시작부터 한 칸을 밟은 셈이 되어 위의 공정성이 깨진다.**

**화면 방향 조작부(D‑pad)를 두지 않는다.** 바닥을 직접 누르면 건너가서 골라지므로 손가락과 마우스에는 그것으로 충분하고, 키보드에는 방향키가 있다. 조작부는 하는 일 없이 자리만 차지했다 — 한때 3×3 패드를 뒀다가 걷어냈으니 **다시 넣지 말 것.** 덕분에 무대는 바닥 높이(165px)만 차지하고 칸이 폭을 다 쓴다.

칸 번호는 좌상단, 정답·오답 표시(`✓` `✗`)는 **우상단**에 절대 배치하고 캐릭터는 아래쪽에 세운다. 위아래로 갈라 두면 칸이 좁아져도 가로로 겹칠 일이 없다. 캐릭터는 칸 높이가 아니라 **칸 아래쪽에서 10px 위**에 발을 맞추므로 칸 높이를 바꿔도 그대로 맞는다.

표시를 «✓ 정답»이 아니라 기호만 쓰는 것도 같은 이유다. 좁은 칸에서 글자가 번호와 부딪힌다. 색이 아닌 것으로도 구분한다는 조건(FR-4.1, FR-4.2)은 기호가 지키고, «정답»·«오답»이라는 말은 위 보기 패널과 피드백 상자에 그대로 있다.

`arena.js`는 **게임 규칙을 갖지 않는다.** 몇 번이 정답인지 모르고, 고른 번호를 `onChoose`로 넘길 뿐이다. 정답 표시도 `quiz.ts`가 알려준 인덱스를 그대로 칠한다. 여기에 채점을 넣으면 `core/session.ts`와 판정이 둘로 갈린다.

**진짜 조작 대상은 위 패널의 보기 버튼이다.** 무대 전체가 `aria-hidden`이고 Tab 순서에도 없다. 스크린리더와 키보드 사용자는 게임 모드와 무관하게 지금까지처럼 보기 버튼을 쓴다. **타일에 `aria-label`을 붙이지 말 것** — 같은 보기가 두 번 낭독된다. 그래서 게임 모드를 꺼도 잃는 기능이 없다.

**조작법은 `?` 버튼의 대화상자에 있다.** 무대에 늘 펼쳐두면 세로를 너무 먹어 옮겼다. 버튼은 퀴즈 헤더의 «나가기» 옆이고 `.quiz--game`일 때만 그린다 — `.arena` 안에 두면 무대 세로를 다시 먹는다. 무대에서 **유일하게 Tab으로 닿는 것**이다: 키보드만 쓰는 사람이 조작법을 볼 길이 있어야 한다. 대화상자가 열려 있으면 `handleDialogKey`가 먼저 받아 숫자키가 뒤로 새지 않는다.

**퀴즈 화면을 떠날 때는 반드시 닫는다** (`arena.closeDialog`, 나가기·결과 보기 양쪽). 대화상자는 `<main>` 뒤에 와서 DOM 순서로 위에 오므로, 열어 둔 채 나가면 **홈 화면 위에 그대로 남아** 화면을 덮고 포커스를 가둔다. 무대를 끄는 `arena.setEnabled(false)`는 게임 모드 토글로만 불리기 때문에 화면 이동만으로는 닫히지 않는다.

칸 좌표는 값을 저장하지 않고 **필요할 때마다 다시 잰다.** 칸 크기가 화면 폭에 따라 달라지기 때문이다. 캐릭터 좌표에는 CSS `transition`을 걸지 않는다 — 매 프레임 JS가 직접 넣으므로 전환을 걸면 입력이 굼떠진다.

**캐릭터는 세 조각이다** — `.walker`(위치, JS가 `transform`으로만 옮긴다) · `.walker__body`(튀고 눌리는 몸통, 눈이나 도트를 품는다) · `.walker__shadow`(땅에 남는 그림자). 몸통을 `::before`로 그리면 눈과 그림자가 따로 놀아 이렇게 갈랐다.

튀는 동작은 **스쿼시 앤 스트레치**다. 닿을 때 납작(`scale: 1.22 0.78`), 솟을 때 길쭉(`0.9 1.12`), 정점에서 제 모양. `transform-origin`이 발밑(`50% 100%`)이라 눌려도 바닥에 붙어 있고, 그림자는 떠 있는 동안 작고 옅어져 높이가 눈에 들어온다. 솟는 구간은 `ease-out`, 떨어지는 구간은 `ease-in`이라 중력처럼 읽힌다.

**`rotate`를 넣지 말 것.** 좌우로 흔들면 튀는 게 아니라 **떠다니는** 것으로 보인다. 한 번 그렇게 만들었다가 되돌렸다.

**동작 클래스는 세 개고 `walker--hop`이 나머지를 이긴다** (`--walking` · `--idle` · `--hop`). 셋의 CSS 명시도가 같아서 파일에 늦게 오는 `--hop`이 이기도록 일부러 맨 뒤에 뒀다 — 고르는 순간의 한 번 뛰기가 걷기·제자리 뛰기에 묻히면 안 되기 때문이다.

**그래서 `pick()`이 붙인 `--hop`은 반드시 떼어야 한다.** `walker-land`는 한 번만 도는 애니메이션이라 끝나면 캐릭터가 굳는데, 클래스가 남아 있으면 `--idle`이 다시 붙어도 계속 가려져 **영영 안 뛴다.**

떼는 자리가 **두 곳**이다.

- `animationend`(`walker-land`) — 끝나는 순간 스스로 뗀다. 루프는 서 있는 동안 멈춰 있으므로 여기서 `--idle`도 되돌려 준다
- `setEnabled` 양쪽 — 숨은 탭에서는 CSS 애니메이션이 멈춰 `animationend`가 오지 않는다. 그때를 위한 보루다

**두 번 다 실제로 물린 자리다.** 처음에는 아무 데서도 떼지 않아 2번 문제부터 캐릭터가 멈췄고, `setEnabled`에서만 떼게 하자 **그 화면에 머무는 자리**(로비의 «만들기»·«새로고침», 랭킹 탭, 앱 바 토글)에서 다시 멈췄다 — 화면을 옮기지 않으면 `setEnabled`가 다시 불리지 않는다.

높이(`translate`)는 **눈속임일 뿐 판정에 쓰지 않는다.** 밟은 칸은 논리 좌표(`pos`)로만 정하므로 튀는 중에 불이 깜빡이지 않는다.

**브라우저 자동화로 검증할 때 주의** — 숨은 탭에서는 rAF가 멈춘다. 캐릭터가 움직이지 않고 타이머도 늦게 발동해 화면과 상태가 어긋나 보인다. 스크린샷을 찍으면 몇 프레임씩 진행되므로, 이동을 확인하려면 키를 누른 채로 스크린샷을 여러 번 찍어야 한다. **버그가 아니다.**

**피드백은 화면 아래에 붙는 시트다.** 답을 고른 뒤 해설과 「다음 문제」를 찾아 스크롤을 내릴 필요가 없고, 엄지가 닿는 자리에 버튼이 온다.

**그래서 `<main>` 밖에 둔다.** 이 프로젝트에는 `z-index`가 하나도 없어 **DOM 순서가 곧 겹치는 순서**다. `<main>` 안에 두면 같은 `fixed` 요소인 스틱·확정 버튼에 가리고, 대화상자들 뒤에 두면 나가기 확인이 시트에 가린다. 지금 자리(`</main>` 다음, 대화상자들 앞)가 그 둘을 동시에 만족한다.

**화면은 `app.ts`의 `goTo()` 로만 옮긴다.** 가는 곳만 남기고 **나머지를 모두 접는다** — `closers` 표에 화면마다 한 줄씩 있고, 화면이 늘어도 거기 한 줄만 더하면 된다. `showScreen`을 직접 부르는 곳은 `goTo` 안 한 곳뿐이다.

**손으로 `hide()` 를 나열하지 말 것.** 그러면 하나씩 빠뜨린다 — 실제로 두 번 물렸다. ① 퀴즈가 빠져 세션과 타이머가 살아남았고, ② 결과·랭킹은 `hide` 자체가 없어 손가락 조작부가 다음 화면까지 따라갔다.

**내용을 그린 뒤에 `goTo` 를 부른다.** 먼저 부르면 아직 비어 있는 화면이 한 번 스친다 — 랭킹이나 대기실처럼 저장소를 기다리는 화면에서 눈에 띈다.

**화면을 옮길 때는 퀴즈를 접는다** (`quizScreen.hide()`). 퀴즈는 오래 제 안에서 나가는 길(나가기·완주)만 정리했는데, **그 길을 거치지 않고 화면이 바뀌면 세션과 타이머가 살아남는다.** 그러면 로비에서 방 이름을 적는 중에 「시간 초과입니다」가 뜨고, 「다음 문제」를 눌러도 `renderQuestion`이 화면을 바꾸지 않아 아무 일도 없어 보이며, 다음 문항의 타이머가 또 돌아 같은 일이 되풀이된다. 홈·로비·대기실·랭킹·내 캐릭터는 이미 저마다 `hide()`가 있었고 **퀴즈만 없었다.**

그 길이 실제로 있었다. 「게임 시작」은 저장소에 알리고 **되돌아온 이벤트를 보고** 판을 여는데(비동기), 그 사이에 대기실을 나가면 `openOnline()`이 먼저 끝나고 `startRound()`가 나중에 세션을 만든다. 그래서 **나간 방의 판도 열지 않는다** (`onStart`에서 `activeRoomCode` 확인) — 늦게 도착한 이벤트로 로비에 있는 사람 앞에서 판이 시작되면 안 된다.

**화면을 떠날 때는 반드시 닫는다** (`hideFeedback`). 시트가 `<main>` 밖에 있어 **퀴즈 화면이 숨겨져도 따라 사라지지 않는다** — 조작법 대화상자와 같은 함정이고, 놓치면 결과 화면이나 홈 위에 그대로 남는다. 닫는 자리는 새 문항(`renderQuestion`)·판 완료(`goNext`)·나가기(`confirmExit`) 셋이다.

**시트 높이만큼 `.app`의 아래 여백을 늘린다** (`--feedback-h`). 시트는 `fixed`라 문서 흐름에서 빠져 있어, 이게 없으면 **마지막 보기가 시트에 가린다.** 높이는 해설 길이에 따라 달라지므로 그릴 때마다 재서 넣고, 닫을 때 `0px`로 되돌린다. 실제 은행의 최장 해설(82자)로도 시트는 화면의 21%이고 `max-height: 60vh`가 상한을 잡는다.

**게임 모드에서 바닥 칸을 가리지 않는다.** 시트가 아래에 붙어 있어 무대와 칸에 칠해진 정답·오답 표시(`✓` `✗`)가 그대로 보인다 — 「내가 밟은 칸이 맞았나」를 해설과 함께 볼 수 있어야 한다. 가운데 모달로 바꾸면 이게 깨진다.

**라이브 리전은 «알리는 말»까지다.** 피드백 상자의 `role="status"`는 판정·해설만 감싸고 「다음 문제」 버튼은 그 밖에 둔다. `role="status"`는 암묵적으로 `aria-atomic`이라 안이 바뀌면 **전체가 한 덩어리로** 낭독되는데, 버튼이 안에 있으면 「정답입니다 … 다음 문제」로 읽히고 곧이어 포커스가 그 버튼으로 가서 같은 말이 또 들린다. 그리고 **숨긴 채로 채우면 낭독되지 않으므로 먼저 펼치고 넣는다** — 라이브 리전을 쓰는 모든 자리(`quiz.ts`의 피드백, `result.js`의 등록 결과, `online.js`의 `say()`)가 같은 규칙이다.

**되돌릴 수 없는 조작 뒤에는 갈 곳을 남긴다.** 랭킹 등록은 한 판에 한 번뿐이라 끝나면 입력칸과 버튼을 잠그는데, 거기 있던 포커스가 `<body>`로 떨어지면 Tab 자리를 잃는다. **잠그기 전에 «어디 있을 것»이라고 넘겨짚지 말고** 잠근 뒤의 실제 상태를 보고 옮긴다 — 폼 제출은 브라우저가 포커스를 옮길 수도 있어 미리 검사하면 맞지 않는다.

**상태 색은 WCAG AA(4.5:1)를 제 배경과 흰 배경 양쪽에서 넘긴다.** 이 색들은 12~13px이나 상속 크기로만 쓰여 «큰 글자» 예외에 드는 곳이 없다. 기호와 글자가 뜻을 지고 색은 거들 뿐이지만(FR-4.1, FR-4.2) **거드는 것도 보여야 거든다.** 색을 고칠 때는 밝기만 낮추고 색상·채도는 그대로 둔다.

**목록 안의 버튼은 글자만으로 부족하다.** 방이 셋이면 Tab으로 「참가, 참가, 참가」만 들린다. `aria-label`에 무엇에 대한 버튼인지를 실어 준다 — 눈으로 보는 사람에게는 글자가 그대로라 잃는 것이 없다.

**타이머와 스크린리더** — 남은 시간 영역은 `aria-hidden="true"`다. 1초마다 갱신되는 값을 낭독하면 문제를 읽을 수 없다. 남은 5초 경고만 문항당 한 번 `announce()`로 알린다. 이걸 "접근성 개선"이라며 라이브 리전에 넣지 말 것.

**`recentQuestionIds`** — 직전 판 문제를 다음 판에서 후순위로 미는 값 (FR-1.4). 2단계에서 PRD 6.3의 localStorage 키로 옮겼고, `preferences`를 거쳐 읽고 쓴다.

**본 문제끼리는 오래된 것부터 낸다.** `sampleQuestions`는 안 본 문제를 먼저 소진하고 모자랄 때만 본 문제를 채우는데, 이때 무작위로 섞지 않고 `recentIds`의 역순(가장 오래전에 본 것부터)으로 낸다. 섞으면 은행이 바닥났을 때 **방금 본 문제가 바로 다음 판에 되돌아온다.** `recentIds`가 최신순이라는 것이 이 정렬의 전제다 — 순서를 뒤집으면 여기가 조용히 반대로 동작한다.

**한 판이 아니라 여러 판을 기억한다** (`RECENT_QUESTION_MEMORY`, 90). 직전 10개만 덮어쓰면 은행이 커져도 같은 문제가 두 판 만에 되돌아온다. 새 ID를 앞에 이어 붙이고 `Set`으로 중복을 걷어낸 뒤 뒤에서 자른다. 다만 **한 카테고리 은행 전체를 덮을 만큼 크게 잡지 말 것** — 전부 «본 문제»가 되면 기억하지 않은 것과 같아진다. 은행을 키우면 이 값도 함께 키운다.

**등급 메시지** — 정답률 기준이다. PRD 예시 `90점↑ 상식왕`은 100점 만점인 카테고리 모드 전제라, 400점 만점인 전체 모드에 점수 절댓값을 쓰면 등급이 아예 안 뜬다. 카테고리 모드 결과는 PRD 예시와 같다.

**전체 모드 출제** — 카테고리마다 `QUESTIONS_PER_CATEGORY_IN_ALL`(5)개씩 뽑아 합친 뒤 전체를 다시 섞는다 (`sampler.buildAllRound`). 은행 전체를 한 통에 넣고 뽑으면 판마다 카테고리별 문항 수가 달라져 결과 화면의 카테고리별 정답 개수를 비교할 수 없다 (FR-5.6). 지금은 25문제·최대 8분 20초로 돌고 만점은 250이다.

**카테고리 모드와 같은 10을 쓰지 않는 이유가 있다.** 카테고리가 다섯이라 10씩 뽑으면 50문제가 되고 한 판이 16분 40초까지 간다. 중간 저장이 없어(새로고침하면 홈) 그만큼 완주가 어렵다. **이 값을 바꾸면 만점이 함께 바뀐다** — 전체 모드 랭킹은 같은 키에 쌓이므로 그 전에 남은 기록이 있으면 나란히 못 본다. 50문제(만점 500)로 돌던 시기가 있었으나 실사용 기록이 없어 그냥 줄였다.

## 저장소 경계 (건드릴 때 특히 주의)

랭킹 저장은 **저장소 어댑터를 통해서만** 호출한다 (FR-6.8, FR-6.9). v2에서 서버 구현체로 갈아끼울 때 게임 로직이 바뀌지 않아야 한다.

- 인터페이스: `saveRecord` / `getRankings` / `getBestScore` / `clearAll`
- 로컬 구현이 동기여도 **모든 메서드는 Promise를 반환**한다
- `localStorage` 직접 호출은 `src/storage/safe-storage.ts` **한 곳에만** 존재한다. 랭킹(`local-store.js`)과 방(`local-rooms.ts`)이 둘 다 그것을 거친다. `core/`나 `ui/`에서 부르면 설계 위반이다
- `core/`와 `ui/`는 저장소를 아예 import하지 않는다. 저장이 필요하면 `app.ts`가 콜백으로 넘긴다
- 교체 지점은 `src/storage/adapter.ts`의 `rankingStore` 한 줄이다
- 닉네임과 `recentQuestionIds`는 `preferences`로 분리했다. v2에서도 브라우저에 남을 값이라 서버로 갈 랭킹 어댑터와 섞지 않는다
- 저장 데이터가 깨져도 앱이 죽지 않고 빈 랭킹으로 복구한다 (PRD 8). 레코드 단위로 검증해 성한 기록은 살린다

**저장소를 아예 못 쓰는 환경도 앱을 죽이지 않는다** (`safe-storage.js`). 사생활 보호 모드처럼
`setItem`이 던지는 브라우저에서는 메모리 대체품으로 넘어간다 — 기록이 남지 않을 뿐 게임은 돈다.
**이 대비를 한쪽에만 두지 말 것.** 한때 랭킹에만 있었고 방 저장소는 직접 `localStorage`를 불렀는데,
그래서 저장이 막힌 브라우저에서는 **「만들기」를 눌러도 아무 일이 없었다** — 방이 저장되지 않아
곧바로 «없는 방»이 됐고, 화면도 안 바뀌고 안내도 없어 버튼이 고장 난 것처럼 보였다.

**쓰기 실패를 삼키기만 하지 않는다.** 대체품으로 넘어가는 것은 «처음부터 막힌» 경우를 덮을 뿐이고,
쓰다가 용량이 차는 경우는 따로다. 방을 여는 길목(`createRoom`·`joinRoom`)은 `writeAll`의 성공 여부를
보고 이유를 화면에 띄운다. 설정 변경이나 한마디는 실패해도 화면이 그대로 돌아 그냥 넘어간다.

**저장이 안 되는 것도 화면에서 숨기지 않는다** (`roomStore.isPersistent`). `isNetworked`와 같은 이유다 —
친구에게 코드를 알려 줬는데 새로고침 한 번에 방이 사라지는 편이, 미리 알려 주는 것보다 나쁘다.

**`questionResults`** — 저장 레코드의 **선택 필드**다. `[{id, correct, timedOut}]` 형태로 문항별 정오를 담는다. 화면에는 쓰지 않고 선생님 모드(`/teacher-dashboard`)가 문항 단위 정답률을 낼 때만 쓴다. PRD 6.2 스키마에 없는 확장이다.

**이 필드 때문에 `SCHEMA_VERSION`을 올리지 않았다.** 올리면 `ensureSchemaVersion`이 기존 랭킹을 통째로 비운다(v1에는 마이그레이션 대상이 없다). 더하기만 하는 변경이라 예전 기록은 이 값 없이, 새 기록은 이 값을 갖고 공존한다. 값이 없거나 깨졌으면 키 자체를 남기지 않는다 — 빈 배열이 "다 틀렸다"로 읽히면 안 된다.

문제 텍스트는 넣지 않는다. `data/*.json`과 중복되고 용량만 늘어난다. 읽는 쪽이 `id`로 이어 붙인다.

점수는 정답당 10점 고정. **속도 보너스나 난이도 배점을 넣지 않는다** — 곱씹을 시간을 보장한다는 PRD 2.1 설계 원칙과 정면으로 충돌한다. 소요 시간은 개인 지표일 뿐 순위에 반영하지 않는다.

## 온라인 로비 (PRD 밖의 확장)

**이 앱에는 서버가 없다.** 정적 파일로만 배포되므로 다른 사람과 방을 나눠 가질 길이 없다.
그래서 **화면과 흐름을 먼저 온전히 만들고, 네트워크 자리는 어댑터로 비워 두었다.**
지금 끼워져 있는 `local-rooms.ts`는 방을 이 브라우저의 localStorage에 담는다.

- 교체 지점은 `src/online/adapter.ts`의 `roomStore` **한 줄**이다 (랭킹 저장소와 같은 방식)
- 인터페이스: `isNetworked` / `me` / `listRooms` / `myRooms` / `createRoom` / `joinRoom` / `leaveRoom`
- 로컬 구현이 동기여도 **모든 메서드는 Promise를 반환한다**
- 방 코드·이름·비밀번호 규칙은 `src/online/rules.ts`에 순수 함수로 둔다. 화면과 저장소가
  **같은 규칙을 쓰게** 하려는 것이다 — 화면에서만 검사하면 서버가 다른 기준으로 거절해 말이 어긋난다

**비밀번호를 목록에 실어 보내지 말 것.** `listRooms`는 잠김 여부(`hasPassword`)만 알린다.
견주는 일은 구현체 안에서 하고 화면은 입력값을 그대로 넘기기만 한다 — 그래야 서버 구현이
됐을 때 그 검사가 **자연히 서버에서** 일어난다. 비밀번호를 내려보내는 순간 잠금은 아무 뜻이 없다.

**진짜로 이어지는지를 화면에서 숨기지 않는다.** `roomStore.isNetworked`를 보고 안내 문구를
고른다. 친구를 불렀는데 아무도 못 들어오는 편이 더 나쁘다.

**방 코드에는 `0·O·1·I·L`을 쓰지 않는다** (`rules.ts`의 `ALPHABET`). 코드는 사람이 불러 주고
받아 적는 것이라 눈으로 헷갈리는 글자를 빼 두었다. 입력은 소문자와 공백도 받아준다.

**«나»는 새로고침해도 그대로여야 한다** (`quiz.playerId`). 페이지를 열 때마다 신분을 새로 만들면
새로고침한 뒤의 내가 방에게는 «다른 사람»이 된다. 들어갔던 자취가 자리를 차지해, 두 번 새로고침하면
**2인용 방이 나 혼자로 가득 차 다시 들어갈 수 없었다.** 서버 구현에서는 계정이나 세션이 이 자리를 대신한다.

**참가는 여러 번 해도 같은 결과다.** 이미 들어가 있으면 거절하지 않고 그대로 들여보낸다 —
새로고침하고 돌아온 것이 «거절»일 이유가 없다. 그래서 자취가 쌓이지 않는다.

**방이 아니라 참가자를 정리한다** (`prune`). 방은 사람이 잠깐 나갔다 와도 남아 있어야 한다 —
새로고침하거나 연결이 끊겼다고 방이 없어지면 남은 사람들이 함께 튕긴다. 대신 오래 안 보인 참가자를
떨구고, 아무도 남지 않은 방만 사라진다. **`seenAt`이 아예 없는 참가자도 떨군다** — 신분을 남기기
전에 만들어진 자취라 주인이 다시 찾아올 수 없다. 서버라면 연결이 끊긴 순간을 알지만 여기서는 알 길이
없어 «마지막으로 본 시각»으로 가늠한다.

**들어간 방에는 «다시 들어갈» 길과 나갈 길을 함께 준다.** 새로고침하거나 홈에 다녀오면 대기실을
떠나 있을 뿐 방에서 나간 것은 아니다. 나갈 길이 없으면 내가 만든 빈 방이 목록에 쌓이고, 다시 들어갈 수도 없다
(서버가 붙으면 남들 목록까지 지저분해진다). 마지막 사람이 나가면 저장소가 방을 지우므로,
**목록에서 사라지는 것 자체가 «나왔다»는 표시**가 된다 — 따로 안내 문구를 띄우지 않는 이유다.

**그 길이 로비 맨 위의 「내가 있는 방」이다** (`myRooms()`). 공개방 목록만으로는 **비공개 방으로 돌아갈
길이 없다** — 거기엔 나오지 않으므로 코드를 적어 두지 않은 사람은 제가 만든 방을 다시 찾지 못한다.
들어가 있는 방은 위로 올리고 **아래 공개방 목록에서는 뺀다**: 같은 방이 두 번 나오면 다른 방인가 싶고,
«참가»와 «들어가기»가 나란히 놓여 뜻이 흐려진다. 하나도 없는 것이 보통이라 빈 안내를 두지 않고
묶음째 감춘다.

**`role="status"` 문단에 `announce()`를 겹쳐 부르지 말 것.** 그 문단이 이미 라이브 리전이라
같은 말이 두 번 낭독된다. 대신 **숨긴 채로 글을 넣으면 낭독되지 않으므로 먼저 펼치고 넣는다.**

### 대기실 (`ui/waiting-room.ts`)

방을 만들거나 참가하면 여기로 온다. 참가자가 바닥에 서 있고 **내 캐릭터만** 그 사이를 걸어 다닌다
— 남이 어디 서 있는지는 서버가 없어 알 길이 없다.

**설정은 «누르면 다음 값으로 도는» 버튼이다.** `select`로 두면 캐릭터가 밟아도 목록이 열리지 않아
«걸어가서 고른다»가 성립하지 않는다. 분야·인원·모드·시작·나가기가 모두 그냥 버튼이라
발밑에 두고 Enter만 누르면 된다. **여기에 `select`를 넣지 말 것.**

**채팅칸과 걷기는 `/`와 `Esc`로 오간다.** 글자를 치는 동안에는 방향키가 커서 것이므로
(`walker.ts`의 `isBlocked`) 둘을 오갈 길이 필요하다. **`Tab`은 가로채지 않는다** —
대기실의 버튼은 캐릭터로 밟아 누를 수 있지만, 키보드만 쓰는 사람에게 Tab으로 버튼에 닿는
길까지 막으면 안 된다. 보내고 나면 스스로 `blur()`해 곧바로 다시 걸을 수 있게 한다.

**말풍선이 화면 위로 넘칠 것 같으면 캐릭터 아래에 띄운다** (`walker__bubble--below`).
캐릭터는 앱 바 버튼을 밟으려고 화면 맨 위까지 갈 수 있어서, 그대로 두면 **말을 해도 화면 밖이라
보이지 않는다.**

**대화는 최근 몇 줄을 화면에 남긴다.** 말풍선은 몇 초 뒤 사라지므로 놓친 말을 다시 볼 길이
있어야 한다. `role="log"`라 이 목록이 곧 라이브 리전이고, **말풍선을 볼 수 없는 사람에게는
여기가 대화 그 자체**다 — `sr-only`로 감추지 말 것.

**설정 버튼은 쓸 수 있는 값만 돈다.** 인원은 지금 있는 사람보다 작게 줄일 수 없는데, 그 값을
건너뛰지 않으면 눌러도 아무 일이 없어 **버튼이 고장 난 것처럼 보인다.** 돌 곳이 없으면 왜
안 되는지 한 줄로 알린다.

**말풍선은 `.walker` 안에 둔다.** 캐릭터를 따라다니게 하려는 것이고, 몸통 애니메이션은
`.walker__body`에 걸려 있어 말풍선은 흔들리지 않는다. 눈으로 보는 장치라 스크린리더에는 닿지
않으므로, 들은 말을 `role="log"` 영역에 남겨 낭독되게 한다.

**방장 판정은 저장소가 한다** (`updateRoom`의 `not-host`). 화면에서만 막으면 서버 구현에서
그대로 뚫린다. 화면은 미리 알려 주려고 버튼을 비활성으로 둘 뿐이다.

**들어가려다 되돌아왔으면 이유를 말한다** (`onLeave(reason)`). 목록을 보는 사이 방이
사라질 수 있는데(마지막 사람이 나가면 저장소가 지운다), 조용히 로비로 되돌리면
**「들어가기를 눌렀는데 아무 일도 없다」**로 보인다. 공개방의 「참가」는 `not-found`로
이미 알려 주고 있어, 여기만 조용하면 **같은 일에 두 가지 얼굴이 된다.**
`reason` 이 없으면 내가 스스로 나간 것이므로 아무 말도 하지 않는다.

**«무슨 일이 있었는지»는 `subscribe`로 듣는다.** 로컬 구현에서는 내가 보낸 말만 나에게
되돌아오지만, 화면은 구독만 보고 그리므로 **서버가 붙어 남의 말이 와도 그리는 코드는 그대로다.**
지금 쓸 데가 없다고 이 자리를 빼 두면 서버를 붙일 때 화면을 다시 짜야 한다.

**방에서 시작한 판은 대기실로 돌아온다** (`app.ts`의 `activeRoomCode`). 그만두든 끝까지 풀든
돌아갈 곳이 방이다 — 결과 화면의 「대기실로」는 방에 들어가 있을 때만 그린다.

**홈으로 가도 방에서 나가지는 않는다.** 방을 떠나는 것은 대기실의 「나가기」뿐이고, 그때
`activeRoomCode`를 비운다. 홈으로 갔다가도 로비에서 코드로 다시 들어갈 수 있다.

**판을 여는 사람이 판을 직접 시작하지 않는다.** 「게임 시작」은 `roomStore.startGame`을 부를 뿐이고,
화면은 되돌아온 `{type: 'match', phase: 'started'}` **이벤트를 보고** 판을 연다. «내가 눌렀는지»를
묻지 않는 것이 핵심이다 — 서버가 붙으면 방장이 누른 시작이 방에 있는 **모두에게** 같은 이벤트로
가고, 그때도 같은 줄이 그대로 판을 연다. 여기서 곧바로 판을 열어 두면 나중에 그 줄을 다시 짜야 한다.

**시작 권한도 저장소가 판정한다** (`not-host`). 화면은 버튼을 잠가 미리 알려 줄 뿐이다.

**아직 없는 자리 — 여럿이 같은 문제를 푸는 판.** `submitAnswer`와
`{type: 'match', phase: 'question' | 'reveal' | 'over'}`가 들어갈 자리를 `online/adapter.js`에
적어 두었다. 진행 규칙(언제 다음 문항으로 넘길지, 점수를 어떻게 합칠지)은 그때 `core/`에 순수
함수로 둔다 — 서버와 화면이 **같은 규칙**을 써야 하기 때문이다. **지금 미리 짓지 않는다** —
쓸 화면이 없으면 맞는지 확인할 길도 없다.

그래서 지금 「게임 시작」은 방 설정으로 **혼자 한 판**을 연다. 방에 나뿐이라 그렇지, 길은 이미
여럿이 지나갈 수 있는 길이다.

## 문제 데이터

`data/*.json`은 PRD 6.1 스키마의 배열이다. **문제 추가는 JSON만 고치면 되고 코드는 손대지 않는다.**

현재 **500문제 (카테고리당 100개로 고르게 맞췄다).** 3-A의 40문제에서 시작해 여러 차례 늘렸다.
**카테고리 모드 10문제, 전체 모드 25문제**로 돈다. 은행을 더 키워도 출제 수는 그대로고
판마다 세트만 달라진다.

**늘릴 때는 배치마다 검증한다.** 500문제쯤 되면 사람 눈으로 중복을 못 잡는다.
스키마·ID 중복만이 아니라 **문항 문장 중복**과 **같은 카테고리 안의 정답 중복**까지 함께 봐야 한다
(정답이 겹치면 물어보는 각도가 같아 한 판에 둘 다 나올 때 반복으로 읽힌다).
난이도 비율과 «정답이 보기 중 가장 긴» 비율(무작위 기대 25%)도 매번 확인한다.

**카테고리마다 같은 수를 유지한다.** 한 카테고리만 얇으면 그쪽을 연달아 풀 때 은행이 금방 바닥나
«최근 문제 후순위»(FR-1.4)가 무의미해진다. 실제로 18개짜리 카테고리가 있을 때 두 판이면 전부
«본 문제»가 됐다. 늘릴 때도 한쪽만 늘리지 말 것.

**카테고리는 다섯이다 — PRD와 어긋난 지점이다.** PRD FR-2.1과 3단계 계획서는 «4개 카테고리»를
전제로 쓰였다. 예술과문화는 그 뒤에 사용자가 결정해 넣은 것이다. PRD는 당시의 기준으로 그대로
두었으니, 문항 수·카테고리 수에서 PRD와 코드가 다르면 **코드가 맞다.** 카테고리를 더하거나 뺄 때
손댈 곳은 아래 세 군데다.

- `src/constants.ts`의 `CATEGORIES` 한 줄. 홈 화면과 랭킹 탭은 여기서 렌더링하므로 자동으로 따라온다
- `data/<코드>.json` 신규
- `.claude/commands/` 열네 파일. 파이썬 스크립트마다 `CATEGORIES` 튜플과 `KO`/`ALIASES` 표가
  하드코딩돼 있다. 슬래시 명령어는 서로 import할 수 없어 생긴 중복이라 함께 고쳐야 한다.
  개수는 어디에도 박아두지 말고 `len(CATEGORIES)`로 센다

CSS는 손댈 필요가 없다. `.category-card:nth-child(odd):last-child`가 홀수 개일 때만 마지막 카드를
두 칸으로 늘린다.

- `id`는 전역 고유. 기존 ID를 재사용하지 않는다 (FR-1.8). 로드 시 카테고리를 넘나들며 중복을 잡는다
- `difficulty`와 `tags`는 v1에서 안 쓰지만 스키마에 넣는다 (v2에서 일괄 수정을 피하려고)
- 앱 시작 시 `data/validate.ts`가 검사하고, 불량 문제는 출제에서 빼고 `console.warn`으로 알린다 (FR-1.9). 앱을 죽이지 않는다

## 문제 작성 규칙

형식 검증(`data/validate.ts`)은 스키마만 본다. **내용의 정확성은 걸러주지 못한다.** 문제를 쓰거나 고칠 때 아래를 매번 확인한다. 논쟁의 여지가 없는 사실만 내고, 해석이 갈리거나 출처마다 다른 내용은 피한다.

1. 정답이 하나뿐인가?
	- 다른 해석 가능 시 조건 명시 (예: 면적 기준, 2024년 기준)
2. 최상급 표현에 기준이 있는가?
	- '가장 큰', '최초의' 등 표현에 측정 기준 명시
3. 시간과 범위가 명확한가?
	- 변할 수 있는 정보는 시점 명시
	- 지리적, 분류적 범위 한정
4. 교차 검증했는가?
	- 의심스러운 정보는 2개 이상 출처 확인
	- 논란 있는 내용은 주류 학설 기준
5. 확신이 서는가?
	- 연도·수치·순위처럼 틀리기 쉬운 항목은 특히 신중하게
	- 확신이 없으면 넣지 말고, 작업이 끝난 뒤 "검증이 필요한 문제" 목록으로 따로 보고한다

보기 품질·난이도·해설 분량·ID 부여 기준은 `docs/quiz-game-claude-code-prompts.md`의 3-A를 따른다.
